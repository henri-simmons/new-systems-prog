#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    // find first number to search through array with and length of array
    int searchNum = atoi(argv[1]);
    int length = argc - 2;
    }